package controller;

import org.junit.Assert;
import org.junit.Test;

public class ControllerTest {
	// private final Controller tester;
	// private final Loop loop;
	// private IView view;
	// private IModel model;

	public ControllerTest() {
		// final Controller controller = new Controller(this.view, this.model);
		// this.tester = new Controller(this.view, null);
		// this.loop = new Loop(null);
	}

	@Test
	public void setLoop() {
		Assert.assertEquals(true, true);
	}

}
